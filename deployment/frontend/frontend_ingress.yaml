apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  #annotations:
    #kubernetes.io/ingress.global-static-ip-name: 34.95.105.77
spec:
  rules:
  - http:
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: frontend-service
            port:
              number: 80
     